import 'mocha';
// import {expect} from 'chai';

describe('pruebas unitarias del ejercicio 4', ()=> {
  it('Se supone que aqui van las pruebas del ejercico 4', ()=> {
    console.log(`No se han podido realizar pruebas con mocha debido a que lo que se hace es recoger datos a través de yargs y visualizar por pantalla tras el calculo`);
  });
});
