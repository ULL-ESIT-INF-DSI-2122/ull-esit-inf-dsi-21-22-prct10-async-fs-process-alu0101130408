import 'mocha';
// import {expect} from 'chai';

describe('pruebas unitarias del ejercicio 1', ()=> {
  it('Se supone que aqui van las pruebas del ejercico 1', ()=> {
    console.log(`Como en el ejercicio 1 solo hay que comentar el funcionamiento de un programa. No se ha podido realizar pruebas unitarias`);
  });
});
